<html>
    <head>
</head>
<body>
<table class="table table-dark">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">name</th>
        <th scope="col">image</th>
        <th scope="col">Action</th>

      </tr>
    </thead>
    <tbody>
      <% vendors.forEach((vendor)=>{%>
        <tr>
          <td><%=vendor.v_id%></td>
          <td><%=vendor.Name%></td>
          <td><img src="/images/<%=vendor.logo%>" style="width:100px;height:100px"/></td>
          <td>
            
            <a class="btn btn-success" id="myData<%=vendor.v_id%>" data-toggle="modal" data-target="#mymodal">MyData</a>
           <!-- <script>
              $(()=>{
                $("#myData<%=vendor.v_id%>").click(()=>{
                  $.ajax({
                    url:"http://localhost:8080/vendors/getData/<%=vendor.v_id%>",
                    method:"get",
                    success:function(result){
                      $("#mydata").html(
                        `<p><img src="/images/${result.image}" style="width:200px;height:200px"/><br/>
                           Name : ${result.Name} <br/>
                         mail :${result.mail} <br/>
                         birthdate : ${result.date} <br/>
                         job : ${result.job} <br/>
                         address : ${result.address.city} - ${result.address.street } - ${result.address.building} <br/>
                         </p> ` 
                      )
                    },
                    error:function(err){
                      alert("error fetching data");
                    }
                  })
                });
                
              });
            </script>
         -->
          
            <a class="btn btn-primary" href="/vendors/edit/<%=vendor.v_id%>">Edit</a>
            <a class="btn btn-danger" href="/vendors/delete/<%=vendor.v_id%>">Delete</a>
          </td>
        </tr>
      <%})%> 
      </td>
    </tbody>
  </table>
</body></html>